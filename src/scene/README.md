# `src/scene`

Empty until roadmap step 20.

Planned contents: React Three Fiber components — canvas, camera rigs, room
shell, and furniture meshes. One Three.js unit is one meter, so no scaling
factor belongs here.

Three.js is deliberately not a dependency yet.
